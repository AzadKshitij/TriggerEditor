# ToDo
- Sidebar
    - [ ] Add a sidebar to the left of the page.
    - [ ] Change sidebar layout based on the input selected. 

- File Input Node
- [ ] Add options to select the file with file browser.
- [ ] Add table like view to show the file content.

- Select Node
    - [ ] Option to select columns from the input data (input node).

- Add Split Data (Test,Train) Node
    - [ ] Add option to select the split ratio.
    - [ ] Add option to select the random seed.
    - [ ] Add option to select the split method (stratified, random).
- Add Data Preprocessing Node
    - [ ] Add option to select the preprocessing method (standardization, normalization, etc.).
    - [ ] Add option to select the columns to preprocess.
    - [ ] Add option to select the preprocessing parameters (mean, std, min, max, etc.).

- [ ] Test all the features for data cleansing node.

- [ ] Add option to do multy input for some nodes.

# Bugs
- [ ] When reading file with file input as a path, the file is read correctly but the node does not register it correctly and also does not pass the data to child nodes.
